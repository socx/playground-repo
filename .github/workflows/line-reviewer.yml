name: Lint Review

on:
  push:
    branches:
      - test-branch
      - main
    paths:
      - '**/*.yaml'
      - '**/*.yml'
      - '**/*.tf'
      - '**/*.md'
  pull_request:
    paths:
      - '**/*.yaml'
      - '**/*.yml'
      - '**/*.tf'
      - '**/*.md'

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y yamllint
          npm install -g markdownlint-cli
          curl -sL https://raw.githubusercontent.com/reviewdog/reviewdog/master/install.sh | sh -s -- -b /usr/local/bin

      # ---------------- YAML ----------------
      - name: Run yamllint with reviewdog
        run: |
          yamllint . --format parsable \
            | reviewdog -f=yamllint -name=yamllint -reporter=github-pr-review -filter-mode=diff -fail-on-error=false

      # ---------------- Terraform ----------------
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Run terraform fmt with reviewdog
        run: |
          terraform fmt -check -diff -recursive \
            | reviewdog -f=diff -name=terraform-fmt -reporter=github-pr-review -filter-mode=diff -fail-on-error=false

      # ---------------- Markdown ----------------
      - name: Run markdownlint with reviewdog
        run: |
          markdownlint '**/*.md' \
            | reviewdog -f=markdownlint -name=markdownlint -reporter=github-pr-review -filter-mode=diff -fail-on-error=false
